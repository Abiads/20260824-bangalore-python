"""
Exercise 6: Grading on a Curve

A professor wants to adjust exam grades.
- Prompt for space-separated test scores
- Convert to list of integers
- Use SINGLE list comprehension with conditionals:
  - If score < 50, add 10 points
  - If score >= 50, add 5 points
  - Cap maximum score at 100

HINTS:
- Use input() to get scores
- Use string.split() to separate scores
- Use list(map(int, ...)) to convert to integers
- Use list comprehension: [expression for score in original_scores]
- Use 'if-else' in comprehension: (10 if score < 50 else 5)
- Use min(100, value) to cap at 100

Example: [45, 88, 30, 98, 50] → [55, 93, 40, 100, 55]
"""

# TODO: Get space-separated scores from user
# TODO: Convert to list of integers
# TODO: Create list comprehension with curve logic
# TODO: Print original and curved scores
def main():


    pass

if __name__== __main__:
    main()